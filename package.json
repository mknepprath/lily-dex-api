{
  "name": "lily-dex-api",
  "version": "1.0.0",
  "description": "Pokemon GO data API for lily dex",
  "type": "module",
  "scripts": {
    "build": "node src/index.js",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
